/**
 * Слой доступа к данным — граница между интерфейсом и источником данных.
 *
 * Сейчас функции читают моки из `src/data`, но объявлены асинхронными, чтобы
 * при подключении реального бэкенда (синхронизация с Shipox) поменялись только
 * тела этих функций, а ни один компонент не пришлось трогать.
 *
 * Что предстоит подменить при интеграции:
 *   listOrders / getOrder   → заказы из Shipox + наши плечи и начисления;
 *   listClients             → справочник контрагентов;
 *   listCouriers            → справочник исполнителей;
 *   listPayments            → оплаты клиентов из бухгалтерии;
 *   правила тарифов и реестры начислений пока живут в браузере (см. lib/store).
 */

export * from "./orders";
export * from "./clients";
export * from "./couriers";
export * from "./payments";
