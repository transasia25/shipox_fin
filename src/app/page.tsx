import { redirect } from "next/navigation";

/**
 * Корень приложения.
 *
 * Куда вести, зависит от роли, а она известна только после входа, поэтому
 * отправляем на форму: вошедшего она сразу перебросит на его первый экран.
 */
export default function Home() {
  redirect("/login");
}
